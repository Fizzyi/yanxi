server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://121.43.238.143:8036/yanxi?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging:
  level:
    org.springframework.security: DEBUG
    com.yanxi.yanxiapi: DEBUG 